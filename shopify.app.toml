# Learn more about configuring your app at https://shopify.dev/docs/apps/tools/cli/configuration
# Payments apps: keep embedded=false. Payment session scopes come from the extension on first deploy.

client_id = "REPLACE_WITH_SHOPIFY_CLIENT_ID"
name = "Autlantic Billing"
application_url = "https://REPLACE_WITH_APP_HOST"
embedded = false

[access_scopes]
scopes = "read_orders,write_orders"

[auth]
redirect_urls = [ "https://REPLACE_WITH_APP_HOST/auth/callback" ]

[webhooks]
api_version = "2025-10"

[pos]
embedded = false
